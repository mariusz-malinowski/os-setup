#!/bin/sh

grep \
  --exclude-dir=node_modules \
  --exclude-dir=logmon_ece-team-configs \
  --exclude-dir=.git \
  --exclude-dir=scala \
  --exclude-dir=target \
  --exclude-dir=build \
  --exclude-dir=.idea \
  --exclude-dir=out \
  --exclude-dir=logs \
  --exclude-dir=jest_* \
  --exclude-dir=assets \
  --exclude-dir=packs* \
  --exclude-dir=deps \
  --exclude-dir=_build \
  --exclude-dir=.elixir_ls \
  --color=auto -rnI ./ -e "$1"
